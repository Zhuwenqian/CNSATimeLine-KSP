# 功能更新

## 文件用途

本文档用于按时间顺序记录 CNSATimeLine 项目的功能新增、Bug 修复和重大变更。每条记录应包含日期、变更类型和简要说明。当文档超过 200 行时，应删除最早的几条记录以保持可读性。

---

### 2026-07-04：添加中英俄三语本地化

- **变更类型**：增强
- **变更内容**：
  - 新增 `Localization/zh-cn.cfg`、`Localization/en-us.cfg`、`Localization/ru.cfg`，将 UI 文本与全部 45 条事件描述纳入 KSP 标准 Localization 系统。
  - 修改 `TimeLineEvent.cs`，新增 `LocalizationKey` 字段以支持本地化键。
  - 重写 `TimeLineDataLoader.cs`：不再读取 `中国航天大事时间线.txt`，改为内置事件元数据（日期、本地化键、精度），运行时通过 `Localizer.Format` 按当前游戏语言读取描述。
  - 修改 `TimeLineWindow.cs`，所有可见文本（窗口标题、按钮、提示、事件描述）均使用 `KSP.Localization.Localizer.Format` 读取，未找到键时回退到 `Description` 字段。
  - 保留 `中国航天大事时间线.txt` 作为历史数据源备份，运行时不再依赖它。

---

### 2026-07-04：修复模组在太空中心无日志/无响应问题

- **变更类型**：修复
- **变更内容**：
  - 在 `CNSATimeLineMod.cs` 中添加静态构造函数，在 DLL 被 CLR 加载时立即输出诊断日志，便于确认 KSP 是否载入本程序集。
  - 添加 `CNSATimeLineMenuMod` 主菜单诊断入口类，用于确认 KSP AddonLoader 能够正常实例化本模组的类。
  - 在 `CNSATimeLineMod.Start()` 中使用 `try-catch` 包裹初始化逻辑，避免异常导致组件被静默销毁。
  - 将 `TimeLineConverter.cs` 中的 C# 7 模式匹配 `is DateTime dt` 改为 C# 5 兼容写法，提升运行时兼容性。
  - 将 `TimeLineDataLoader.cs` 中的 `out DateTime eventTime` 改为先声明变量，提升运行时兼容性。

---

### 2026-07-04：太空中心时间线 UI 与自动加速

- **变更类型**：新增
- **变更内容**：
  - 将 `CNSATimeLineMod.cs` 从飞行场景入口改为太空中心（SpaceCentre）场景入口。
  - 新增 `TimeLineEvent.cs`，定义时间线事件数据结构。
  - 新增 `TimeLineConverter.cs`，通过反射读取 `RSSTimeFormatter` 的 epoch，实现真实日期时间与 KSP UT 秒数的映射。
  - 新增 `TimeLineDataLoader.cs`，解析 `中国航天大事时间线.txt` 为事件列表。
  - 新增 `TimeLineWindow.cs`，实现太空中心场景的 IMGUI 窗口，支持 `Ctrl+L` 打开/关闭。
  - 每个事件旁显示“加速到此时”按钮，点击后通过 `TimeWarp.fetch.WarpTo` 加速到事件时间前 10 小时；不可倒退加速的按钮自动置灰。
  - 更新 `CNSATimeLine.csproj` 以包含新增源文件，并补充 `UnityEngine.InputLegacyModule` 引用。
  - 更新 `readme/开发文档.md`、`readme/实施文档.md` 与 `readme/功能更新.md`。

---

### 2026-07-04：项目初始化

- **变更类型**：新增
- **变更内容**：
  - 初始化 KSP 1.12.5 模组开发环境。
  - 创建 `CNSATimeLine.csproj`，目标框架为 .NET Framework 4.7.2。
  - 配置对 `KSP DLL/` 目录下 KSP 与 Unity 程序集的引用。
  - 配置对 `RSSTimeFormatter.dll` 的引用，并设置 `Private=false` 避免构建输出冗余。
  - 创建 `CNSATimeLineMod.cs` 基础模组入口类示例，演示 RSSTimeFormatter 依赖检测。
  - 创建 `readme/` 目录及开发文档、实施文档、功能更新文档。
